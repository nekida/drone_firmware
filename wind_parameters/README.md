## Description
Библиотека для сохранения и извлечения параметров моторов дрона

## Build
`mkdir build`

`cd build/`

`cmake ..`

`make`

## Test
`cd build/`

`ctest .`

или

`cmake . && cmake --build . && ctest .`